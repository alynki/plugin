{
  "name": "alynki",
  "version": "0.9.0",
  "description": "Alynki — the business context layer: governed context, workstreams and gates, inherited into every session. The MCP connection itself is configured separately with `claude mcp add` — see the setup skill.",
  "author": {
    "name": "Alynki"
  }
}
